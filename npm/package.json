{
  "name": "comment-graph",
  "version": "0.1.1",
  "description": "CLI wrapper for comment-graph bundling a native binary",
  "bin": {
    "comment-graph": "./bin/comment-graph.js"
  },
  "scripts": {
    "build": "GOOS=linux GOARCH=amd64 go build -C .. -o npm/bin/comment-graph ./cmd/comment-graph",
    "prepack": "npm run build"
  },
  "files": [
    "bin"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kuri-sun/comment-graph.git"
  },
  "engines": {
    "node": ">=16"
  }
}
