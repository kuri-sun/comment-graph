project_name: comment-graph

before:
  hooks:
    - go test ./...

builds:
  - id: comment-graph
    main: ./cmd/comment-graph
    binary: comment-graph
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: comment-graph
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE*
      - README.md

checksum:
  name_template: "checksums.txt"

changelog:
  use: git
